<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="keywords" content="HTML5, Responsive" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>Your Saved Waifu</title>
    <meta name="description" content="Saved Waifu" />
    <link rel="stylesheet" type="text/css" href="/css/style.css" />
  </head>
  <body>
    <h1>Here is your Saved Waifu!</h1>
    <div>Welcome back, <%= name %>!</div>

    <% if (waifuImageUrl) { %>
      <div>
        <img src="<%= waifuImageUrl %>" alt="Waifu Image" />
      </div>
    <% } else { %>
      <div>No waifu found for this username.</div>
    <% } %>

    <br><br>
    <a href="/">Go back</a>
  </body>
</html>
