<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="/css/style.css" />
    <title>Waifu Match</title>
</head>
<body>
    <h1>Thank You, <%= name %>!</h1>
    <p>Your preference for "<%= tag %>" has been saved.</p>
    <h2>Here is a Waifu Just for You!</h2>
    <% if (waifuImage) { %>
        <img src="<%= waifuImage %>" alt="Your Waifu" style="max-width: 400px;">
    <% } else { %>
        <p>Sorry, no waifus were found matching your type. Try again later!</p>
    <% } %>
    <a href="/">Back to Home</a>
</body>
</html>
